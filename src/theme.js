import { createTheme } from "@mui/material/styles";

export default createTheme({
  palette: {
    light: {
      main: "hsl(0, 0%, 100%)",
    },
    dark: {
      main: "hsl(209, 23%, 22%)",
    },
    background: {
      default: "#fff",
    },
  },
});
